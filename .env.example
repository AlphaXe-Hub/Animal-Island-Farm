# 复制为 .env 后由 docker compose 读取（可选）
# 若本机 8080 已被占用，改为 8081 等
WEB_PORT=8081
JWT_SECRET=abcdefghijklmnopqrstuvwxyz0123456789

# 本地开发（frontend Vite）可使用：
# VITE_API_BASE_URL=http://localhost:3000

# Mongo 仅用于本地脚本直连时：
# MONGODB_URI=mongodb://localhost:27017/animal-island-farm
